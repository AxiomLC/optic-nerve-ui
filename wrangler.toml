name = "optic-nerve-ui"
pages_build_output_dir = "dist"
compatibility_date = "2025-04-01"

# All env vars are in .env.production (committed in repo) — Vite bakes them into
# the JS bundle at build time. No Cloudflare dashboard vars needed.
